- var numFormat = new Intl.NumberFormat('en-US', {maximumFractionDigits: 2});
- var currencyFormat = new Intl.NumberFormat('en-US', { style: 'currency', currency: 'USD', minimumFractionDigits: 0, maximumFractionDigits: 0 });
| #{data.current.Hashtag} unofficial wildfire update: #{numFormat.format(data.current.DailyAcres)} Acres. #{numFormat.format(data.current.PercentContained)}% Contained*.#{'\n'} 
if data.cities
  if data.cities.closest
    | #{data.cities.closest.directions + '. '}
  if data.cities.biggest
    | #{data.cities.biggest.directions + '. '}
| May be incorrect. Consult officials for safety info. Disclaimers in images.