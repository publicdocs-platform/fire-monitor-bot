- var numFormat = new Intl.NumberFormat('en-US', {maximumFractionDigits: 2});
- var currencyFormat = new Intl.NumberFormat('en-US', { style: 'currency', currency: 'USD', minimumFractionDigits: 0, maximumFractionDigits: 0 });
- const friendlyDate = (d) => new Date(d).toLocaleString('en-US', {timeZone:'UTC', hour12:true, year: 'numeric', day: 'numeric', month: 'short', day: 'numeric', hour: 'numeric', minute: 'numeric', timeZoneName: 'short'});
if (data.current.NFSAType || '').includes('Emerging')
  | !{data.current.Hashtag} 🔥 unofficial new fire report.#{'\n'}
else
  | !{data.current.Hashtag} 🔥 unofficial update.#{'\n'}
if data.cities
  if data.cities.closest
    | !{data.cities.closest.directions + '. '}
| ⚠️ DO NOT USE FOR SAFETY PURPOSES - See officials instead. May be incorrect; disclaimers in images.